version: 1
applications:
  - appRoot: "KC_UI/src"
    frontend:
      phases:
        preBuild:
          commands:
          - node -v
          - npm -v
          - npm install
        build:
          commands:
            - npm run build
            - ls -alh
      artifacts:
        baseDirectory: dist/kickConnect/browser
        files:
          - '**/*'
      cache:
        paths: